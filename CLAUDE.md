# 반도체 뉴스 판단 에이전트 — 프로젝트 컨텍스트

이 문서는 Routines(일일 자동 실행)가 이 폴더에서 세션을 시작할 때 참고하는 컨텍스트다.
전체 설계는 [spec.md](spec.md) 참고. 여기서는 "매일 무엇을, 어떤 기준으로 하는가"만 다룬다.

## 일일 파이프라인 (구현 진행 상황: 1~5단계 완료)

### 1~2단계: RSS 수집 + 규칙 기반 1차 분류

```bash
python scripts/collect.py
```

`data/candidates/{날짜}.json`에 후보 기사 목록이 생성된다 (relevance/confidence 없음).
소스 9개(전자신문/ZDNet Korea/디일렉/IT조선/아이뉴스24/블로터/테크M/한국경제/EBN)를
키워드 필터로 거른 뒤 제목 유사도로 중복 제거한 결과다. 1차 필터는 "명백히 무관한 것"만
거르는 용도라 노이즈가 일부 섞여 있는 게 정상이며, 그 판단은 3단계 AI가 한다.

### 3단계: AI 관련도 판단 (Claude Code 세션이 직접 수행 — API 호출 아님)

`data/candidates/{날짜}.json`을 읽고, 각 후보에 대해 아래 기준으로 판단해
`data/{날짜}.json`을 spec.md 3절 스키마대로 작성한다:

**관점**: SK하이닉스 메모리·공정기술 직무 관점. HBM/D램/낸드 기술·사업, 반도체 공정
(식각/증착/CMP/패키징/후공정 등), 경쟁사(삼성전자·마이크론·TSMC)의 기술·실적, 주요
고객사(엔비디아 등)의 AI 반도체 수요, 장비·소재 공급망(ASML·Lam Research 등)을 중심으로
본다.

**relevance (0-100) 기준**:
- 70-100: SK하이닉스가 직접 언급되거나, 메모리(HBM/D램/낸드) 기술·경쟁사 동향을 실질적으로
  다룸
- 40-69: 반도체 업계 전반에 영향은 있으나 간접적이거나(파운드리 경쟁, 정책, 소부장 개별
  계약 등) 메모리 사업과의 연결이 명시적이지 않음
- 0-39: 키워드가 우연히 걸렸을 뿐 실질적으로 무관함(증시 시황, 인물 동정, 비유적 표현 등)

**confidence (high/medium/low) — relevance 점수와 별개로 판단할 것**:
- high: 관련 있음/없음이 명확함
- medium: 어느 정도 근거는 있으나 판단이 다소 애매함
- low: 정말 판단하기 어려운 경계 사례(본문 부족, 비유적 표현과 실제 의미가 헷갈리는 경우
  등). 하루에 1~2건 정도만 나오는 게 정상이며, 이 항목이 이메일/사이트에서 강조 표시된다.

**참고**: 같은 행사를 여러 매체가 다른 제목으로 보도하면 제목 유사도 기반 중복 제거가
놓칠 수 있다(예: SK하이닉스 임원 발표를 4개 매체가 각기 다른 제목으로 보도). 지금은
허용하고 있고, 판단 시 동일 이벤트임을 인지하되 각 기사는 개별적으로 relevance/confidence를
매긴다.

### 4단계: confidence "low" 항목 강조 표시

`scripts/lib/render.py`의 `render_article_card()`가 카드 하나를 HTML로 렌더링한다.
confidence가 "low"인 카드만 노란 배경(`#fffbeb`) + 왼쪽 amber 테두리 + "⚠ 확신 낮음"
배지로 시각적으로 구분된다. 이 렌더링 함수는 5단계(이메일/사이트)에서 그대로 재사용한다.
카테고리(기사/발표/기타)는 배지 색으로만 구분하고 강조 대상은 아니다.

미리보기: `python scripts/preview.py [날짜]` → `site/preview.html` 생성.

### 5단계: 이메일 리포트 + 사이트 발송

**이메일은 개별 기사 카드 나열이 아니라, Claude Code 세션이 종합해서 쓰는 리포트 한 건이다**
(사용자 확정 사항 — 카드 45개를 그대로 나열하면 신호 대비 노이즈가 많아서, 요약·정리·분류를
거친 리포트 형태를 요청함). `data/{날짜}.json`(3단계 판단 결과)을 바탕으로
`data/{날짜}.report.html`을 작성한다 (body에 들어갈 HTML 조각만, `<html>`/`<body>` 태그 없이):

1. **오늘의 요약** — 2~4문장. 오늘 가장 두드러진 흐름(예: 같은 이벤트를 여러 매체가
   보도했다면 그 사실과 핵심 내용, 경쟁사 간 대비되는 전략 등)을 짚는다. SK하이닉스·삼성전자가
   주 내용인 기사는 지금 수준으로 간단히 언급하면 충분하다.
2. **핵심 뉴스 (관련도 70+)** — 항목별로 정리하되, **같은 이벤트를 여러 매체가 보도한 경우
   하나로 묶고 출처 링크를 모아서 표기**한다 (기계적으로 45건을 나열하지 않는다).
3. **주요 업계 동향 (관련도 40~69)** — 개별 나열이 아니라 주제별(메모리 수요·가격 /
   경쟁사 동향 / 패키징·소부장 / AI 반도체 생태계 등)로 묶어 정리한다.
4. 관련도 40 미만(노이즈성)은 리포트 본문에는 넣지 않고, "전체 N건은 사이트에서 확인"
   문구로 안내한다.

**confidence "low" 항목은 이메일 리포트에는 넣지 않는다** (사용자 확정 사항 — 리포트를
간결하게 유지하기 위해 뺐다). 강조 표시는 사이트에서만 한다(4단계, 카드 노란 배경).
이메일 상단 통계 줄의 "확신 낮음 N건" 숫자는 사이트로 유도하는 용도로 유지한다.

이메일 제목은 `[반도체 뉴스] {날짜} 리포트` 형식이며 괄호 등 부가 정보를 붙이지 않는다.

참고 구현: [data/2026-08-25.report.html](data/2026-08-25.report.html)를 템플릿처럼 참고할 것.

```bash
python scripts/build_site.py           # site/index.html + site/archive/{날짜}.html 생성 (카드 전체 + 피드백 버튼)
python scripts/send_email.py           # dry-run: data/{날짜}.email-preview.html 생성, 실발송 안 함
python scripts/send_email.py --send    # 실제 Gmail SMTP 발송 (.env 자격증명 사용)
```

- 사이트(`build_site.py`)는 리포트와 별개로 45건 카드 전체 + 👍/👎 피드백 버튼을 그대로
  보여준다. `render.py`의 `include_feedback=True`로 카드마다 버튼을 넣고, 클릭 시
  `lib/config.py`의 Google Form(`FORM_SUBMIT_URL`/`FORM_ENTRY_ARTICLE_ID`/`FORM_ENTRY_VOTE`)으로
  `mode:no-cors` POST를 보낸다(스펙 4절).
- `send_email.py`는 `data/{날짜}.report.html`이 없으면 발송하지 않고 안내만 출력한다 —
  리포트를 먼저 작성해야 한다.
- `.env`에 `GMAIL_ADDRESS`/`GMAIL_APP_PASSWORD`/`EMAIL_TO` 필요. `send_email.py`는 기본이
  dry-run이고 `--send`를 명시해야 실제로 발송한다(오발송 방지).
- `SITE_BASE_URL`(`lib/config.py`)은 GitHub Pages 배포 후 실제 도메인과 일치해야
  이메일의 "사이트에서 보기" 링크가 정확히 연결된다.

### 6단계 (미구현)

- git commit & push 자동화 (구현 예정)
